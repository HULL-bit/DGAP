from django.apps import AppConfig


class RhConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.rh"
    label = "rh"
    verbose_name = "Ressources humaines (M8) et demandes internes (M7) — Bloc F/G"
