from django.apps import AppConfig


class DetenusConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.detenus"
    label = "detenus"
    verbose_name = "Détenus et mouvements (M10) — Bloc G, JAMAIS exposé public"
