from django.apps import AppConfig


class IntranetConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.intranet"
    label = "intranet"
    verbose_name = "Tableau de bord agent, notes de service (M7) — Bloc F"
