from django.apps import AppConfig


class GedConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.ged"
    label = "ged"
    verbose_name = "Gestion électronique des documents (GED, M6) — Bloc G, non exposé public"
