from django.apps import AppConfig


class DemarchesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.demarches"
    label = "demarches"
    verbose_name = "Espace démarches, contact tracé, FAQ — Bloc B"
