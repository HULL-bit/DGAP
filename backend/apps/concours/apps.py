from django.apps import AppConfig


class ConcoursConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.concours"
    label = "concours"
    verbose_name = "Téléservice Concours (M4) — Bloc E"
