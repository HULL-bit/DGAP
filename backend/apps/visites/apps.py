from django.apps import AppConfig


class VisitesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.visites"
    label = "visites"
    verbose_name = "Téléservice Visites (M3) — Bloc D"
