from django.apps import AppConfig


class ContenusConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.contenus"
    label = "contenus"
    verbose_name = "CMS éditorial (pages, articles, menus, médias, workflow) — Bloc C"
